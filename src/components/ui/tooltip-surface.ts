/** Shared look for short icon hover labels - matches simple Toast messages. */
export const TOOLTIP_SURFACE_CLASS =
  'pointer-events-none fixed z-[130] w-max max-w-[min(16rem,calc(100vw-2rem))] rounded-full bg-primary px-4 py-2 text-sm font-medium text-white shadow-xl leading-snug';
